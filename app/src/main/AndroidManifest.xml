<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.car2go">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Car2GO">
        <activity android:name=".ui.payment.ConfirmRentalActivity"></activity>
        <activity android:name=".ui.payment.AddPaymentActivity" />
        <activity android:name=".ui.payment.EditDriverDetailsActivity" />
        <activity android:name=".ui.payment.AddDetailsActivity" />
        <activity android:name=".ui.payment.ForgotPasswordActivity" />
        <activity android:name=".ui.payment.SignUpActivity" />
        <activity android:name=".ui.payment.LoginActivity" />
        <activity android:name=".ui.rentCar.SubmittedActivity"></activity>
        <activity android:name=".ui.rentCar.SummaryActivity" />
        <activity android:name=".ui.rentCar.CarActivity" />
        <activity android:name=".ui.rentCar.RentCarActivity" />
        <activity android:name=".ui.services.ContactUs"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->


        <activity android:name=".ui.services.AddRating" />
        <activity android:name=".ui.services.AddInquiry" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Car2GO.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>